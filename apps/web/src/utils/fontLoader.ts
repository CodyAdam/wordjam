import { Lilita_One, Niramit } from 'next/font/google';

export const boardFont = Lilita_One({
  weight: '400',
  subsets: ['latin'],
});

export const uiFont = Niramit({
  weight: ['200', '300', '500', '700', '600'],
  subsets: ['latin'],
});
